#include "monty.h"

/**
 * nop - Does nothing.
 * @stack: A pointer to the top of the stack (unused).
 * @line_number: The line number where the nop function is called (unused).
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
